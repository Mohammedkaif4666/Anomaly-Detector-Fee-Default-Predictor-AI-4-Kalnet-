services:
  - type: web
    name: kalnet-ai4
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn jyothsna_lenka.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
